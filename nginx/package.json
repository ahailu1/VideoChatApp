{
  "name": "nginx",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "initdocker": "docker container run --name nginx_container --rm --interactive -v image_volume:/srv -v nginx_volume:/etc/nginx/conf.d -p 80:80 nginx_image", 
    "initcontainer": "docker container start --interactive nginx_container",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
